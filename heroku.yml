setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
run: 
  web: gunicorn main:welcome



#build:
  #docker:
    #web: Dockerfile
#run:
  #web: bundle exec puma -C config/puma.rb